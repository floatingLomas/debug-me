{
  "name": "debug-me",
  "version": "0.0.1",
  "description": "A wrapper around `debug` that uses the current file's path as the `debug` name",
  "main": "index.js",
  "scripts": {
    "test": "mocha test --recursive"
  },
  "author": "Jonathan Lomas <jonathan@floatinglomas.ca>",
  "license": "ISC",
  "dependencies": {
    "app-root-dir": "^1.0.2",
    "debug": "^2.2.0"
  },
  "devDependencies": {
    "mocha": "^2.4.5",
    "pre-commit": "^1.1.2",
    "should": "^8.2.1",
    "strip-ansi": "^3.0.0"
  }
}
